

// import express from 'express';
// import { getRoles } from '../Controllers/RolesContoller.mjs';
// import verifyToken from '../Middleware/verifyToken.mjs';  // Import the token verification middleware

// const router = express.Router();
// router.get('/roles-list', getRoles);
// router.get('/roles', verifyToken, getRoles);  // Protect this route with token verification

// export default router;